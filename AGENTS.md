# Toby
Native, voice-first macOS personal workspace. Preserve the dark editorial design.
Use focused SwiftUI views, Observation, and explicit service ownership. No chat sidebar.
Do not launch the app, run functionality tests, control devices, or perform visual QA unless Rohan explicitly asks. Compile checks are allowed.
Never modify or migrate the old Toby application's data. This app uses a separate Application Support directory and bundle identifier.
Keep version, changelog, and architecture documentation current. Never commit secrets or build products.
